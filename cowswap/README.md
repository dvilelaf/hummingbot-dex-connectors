# CoW Swap Connector MVP

Python components for a Hummingbot-style CoW Swap connector.

The connector is intended to run in the Hummingbot API/runtime layer. Marlin
should consume it only through standard Hummingbot API connector surfaces.
